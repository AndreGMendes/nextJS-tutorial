// --------------------------------------------------------------------------
// THIS IS A PLACE FOR ALL THE GENERIC FUNCTIONS USED ACROSS DIFFERENT PAGES
// --------------------------------------------------------------------------

const getGenericPageName = (pageName: string): string => {
  console.log(pageName);
  return pageName;
};

export { getGenericPageName };
